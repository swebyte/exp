# Explicit static routes first (prerendered)
 /about        /about           200
 /blog         /blog            200
 /experience   /experience      200
 /profile      /profile         200

# SPA fallback for everything else (dynamic routes like /blog/5)
 /*            /index.html      200
